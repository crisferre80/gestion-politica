import React from 'react';

// Stub eliminado: el sistema de calificaciones fue retirado del frontend.
// Conservamos un componente nulo para evitar roturas si queda alguna referencia remanente.
const RecyclerRatingsModal: React.FC = () => null;

export default RecyclerRatingsModal;
